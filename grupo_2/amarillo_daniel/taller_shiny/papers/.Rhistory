ref_row <- tibble(doi = doi,
list(ref_ex))
dyn_cit$session$close()
return(list(paper_row,abstract_row,autores_row,ref_row))
}
shiny::runApp()
runApp()
extraer_general("https://www.akjournals.com/view/journals/2006/2006-overview.xml?contents=latestArticles-30643")
paquetes <- c(
"rvest", "xml2", "httr2", "dplyr", "stringr","openalexR",
"purrr", "tibble", "janitor", "readr", "knitr", "tidyr","RSQLite"
)
# Verificamos qué paquetes faltan
instalados <- rownames(installed.packages())
pendientes <- setdiff(paquetes, instalados)
if (length(pendientes) > 0) {
install.packages(pendientes)
}
# Cargamos los paquetes sin mostrar mensajes
lapply(paquetes, library, character.only = TRUE)
#URLs DE JOURNALS IMPRESOS
issues_url <- c("https://www.akjournals.com/view/journals/2006/14/1/2006.14.issue-1.xml"
,"https://www.akjournals.com/view/journals/2006/14/2/2006.14.issue-2.xml"
,"https://www.akjournals.com/view/journals/2006/14/3/2006.14.issue-3.xml"
,"https://www.akjournals.com/view/journals/2006/14/4/2006.14.issue-4.xml"
,"https://www.akjournals.com/view/journals/2006/15/1/2006.15.issue-1.xml")
crear_nodo <- function(url){
# Definimos un user-agent similar al de un navegador real
user_agent_navegador <- paste(
"Mozilla/5.0 (Windows NT 10.0; Win64; x64)",
"AppleWebKit/537.36 (KHTML, like Gecko)",
"Chrome/122.0.0.0 Safari/537.36"
)
# Construimos la solicitud HTTP y añadimos encabezados para
# que parezca una visita normal
respuesta <- request(url) |>
req_user_agent(user_agent_navegador) |>
req_headers(`accept-language` = "en-US,en;q=0.9") |>
req_perform()
# Convertimos el cuerpo de la respuesta en un documento HTML
# analizable con rvest/xml2
nodo <- respuesta |>
resp_body_html()
nodo
}
extraer_general <- function(nodo) {
# Extraemos el títulos de los articulos
titulo <- nodo |>
html_elements("[data-testid='block-primitivetitle']") |>
html_text2()
issue <- titulo[length(titulo)]
titulo <- titulo[-length(titulo)]
# Extraemos el enlace relativo doi del articulo
doi <- nodo |>
html_elements( " a,[target='_blank'], .c-Button--link") |>
html_text()
#filtramos por links que contengan https doi
doi <- doi[grep(pattern="https://doi*",doi)]
#Creamos una tabla con la información general de cada journal
tibble(
titulo = titulo,
doi = doi,
issue = issue
)
}
extraer_general("https://www.akjournals.com/view/journals/2006/2006-overview.xml?contents=latestArticles-30643")
a <- crear_nodo("https://www.akjournals.com/view/journals/2006/2006-overview.xml?contents=latestArticles-30643")
extraer_general(a)
# Extraemos el títulos de los articulos
titulo <- a |>
html_elements("[data-testid='block-primitivetitle']") |>
html_text2()
issue <- titulo[length(titulo)]
titulo <- titulo[-length(titulo)]
# Extraemos el enlace relativo doi del articulo
doi <- a |>
html_elements( " a,[target='_blank'], .c-Button--link") |>
html_text()
#filtramos por links que contengan https doi
doi <- doi[grep(pattern="https://doi*",doi)]
#Creamos una tabla con la información general de cada journal
tibble(
titulo = titulo,
doi = doi,
issue = issue
)
doi
issue
titulo
titulo <- titulo[length(titulo)]
titulo
titulo <- titulo[-length(titulo)]
titulo
# Extraemos el títulos de los articulos
titulo <- a |>
html_elements("[data-testid='block-primitivetitle']") |>
html_text2()
issue <- titulo[length(titulo)]
issue
titulo
issue <- NA
titulo <- titulo[-length(titulo)]
# Extraemos el enlace relativo doi del articulo
doi <- a |>
html_elements( " a,[target='_blank'], .c-Button--link") |>
html_text()
#filtramos por links que contengan https doi
doi <- doi[grep(pattern="https://doi*",doi)]
#Creamos una tabla con la información general de cada journal
tibble(
titulo = titulo,
doi = doi,
issue = issue
)
titulo
doi
# Extraemos el títulos de los articulos
titulo <- a |>
html_elements("[data-testid='block-primitivetitle']") |>
html_text2()
issue <- NA
titulo <- titulo[length(titulo)]
# Extraemos el enlace relativo doi del articulo
doi <- a |>
html_elements( " a,[target='_blank'], .c-Button--link") |>
html_text()
#filtramos por links que contengan https doi
doi <- doi[grep(pattern="https://doi*",doi)]
#Creamos una tabla con la información general de cada journal
tibble(
titulo = titulo,
doi = doi,
issue = issue
)
titulo <- titulo[-length(titulo)]
# Extraemos el enlace relativo doi del articulo
doi <- a |>
html_elements( " a,[target='_blank'], .c-Button--link") |>
html_text()
#filtramos por links que contengan https doi
doi <- doi[grep(pattern="https://doi*",doi)]
#Creamos una tabla con la información general de cada journal
tibble(
titulo = titulo,
doi = doi,
issue = issue
)
titulo
length(titulo)
# Extraemos el títulos de los articulos
titulo <- a |>
html_elements("[data-testid='block-primitivetitle']") |>
html_text2()
issue <- NA
length(titulo)
titulo <- titulo[-length(titulo)]
# Extraemos el enlace relativo doi del articulo
doi <- a |>
html_elements( " a,[target='_blank'], .c-Button--link") |>
html_text()
#filtramos por links que contengan https doi
doi <- doi[grep(pattern="https://doi*",doi)]
#Creamos una tabla con la información general de cada journal
tibble(
titulo = titulo,
doi = doi,
issue = issue
)
titulo <- titulo[length(titulo)]
titulo
titulo <- titulo[1:length(titulo)]
titulo
titulo
titulo <- titulo[C(1:length(titulo))]
titulo <- titulo[1,length(titulo)]
titulo[length(titulo)]
1:15
titulo <- titulo[1:length(titulo)]
titulo
titulo <- titulo[.1:length(titulo)]
titulo
titulo <- titulo[,1:length(titulo)]
titulo <- titulo[1:length(titulo),]
titulo <- titulo |> select(1:length(titulo)
titulo
titulo
# Extraemos el títulos de los articulos
titulo <- a |>
html_elements("[data-testid='block-primitivetitle']") |>
html_text2()
titulo <- titulo |> select(1:length(titulo)
titulo
titulo |> select(1:length(titulo))
titulo
typeof(titulo)
titulo
# Extraemos el títulos de los articulos
titulo <- a |>
html_elements("[data-testid='block-primitivetitle']") |>
html_text2()
issue <- NA
# Extraemos el enlace relativo doi del articulo
doi <- a |>
html_elements( " a,[target='_blank'], .c-Button--link") |>
html_text()
#filtramos por links que contengan https doi
doi <- doi[grep(pattern="https://doi*",doi)]
#Creamos una tabla con la información general de cada journal
tibble(
titulo = titulo,
doi = doi,
issue = issue
)
runApp()
View(a)
extraer_general(a)
extraer_general <- function(nodo) {
# Extraemos el títulos de los articulos
titulo <- a |>
html_elements("[data-testid='block-primitivetitle']") |>
html_text2()
issue <- NA
# Extraemos el enlace relativo doi del articulo
doi <- a |>
html_elements( " a,[target='_blank'], .c-Button--link") |>
html_text()
#filtramos por links que contengan https doi
doi <- doi[grep(pattern="https://doi*",doi)]
#Creamos una tabla con la información general de cada journal
tibble(
titulo = titulo,
doi = doi,
issue = issue
)
}
a <- crear_nodo("https://www.akjournals.com/view/journals/2006/2006-overview.xml?contents=latestArticles-30643")
extraer_general(a)
b <- extraer_general(a)
extraer_general <- function(nodo) {
# Extraemos el títulos de los articulos
titulo <- a |>
html_elements("[data-testid='block-primitivetitle']") |>
html_text2()
issue <- "Sin Issue"
# Extraemos el enlace relativo doi del articulo
doi <- a |>
html_elements( " a,[target='_blank'], .c-Button--link") |>
html_text()
#filtramos por links que contengan https doi
doi <- doi[grep(pattern="https://doi*",doi)]
#Creamos una tabla con la información general de cada journal
tibble(
titulo = titulo,
doi = doi,
issue = issue
)
}
a <- crear_nodo("https://www.akjournals.com/view/journals/2006/2006-overview.xml?contents=latestArticles-30643")
b <- extraer_general(a)
b
b |> anti_join(,papers,by= doi)
b |> anti_join(papers,by= doi)
b |> anti_join(papers,by= "doi")
b |> anti_join(papers)
papers
#### carga de datos y normalización ####
con <- dbConnect(RSQLite::SQLite(), "JBA_25_26.sqlite")
for( i in dbListTables(con)){
assign(i,dbReadTable(con, i))
}
papers <- papers |> mutate(issue = str_remove(issue,".*:"))
b |> anti_join(papers)
b |> anti_join(papers,by=doi)
papers
b
View(papers)
for(i in b){
nodo <- crear_nodo(b)
extraer_paper(nodo)
}
for(i in b$doi){
nodo <- crear_nodo(b)
extraer_paper(nodo)
}
print(i)
for(i in b$doi){
print(i)
nodo <- crear_nodo(b)
extraer_paper(nodo)
}
rlang::last_trace()
crear_nodo("https://doi.org/10.1556/2006.2025.00590")
for(i in b$doi){
print(i)
nodo <- crear_nodo(b)
extraer_paper(nodo)
}
for(i in b$doi){
print(i)
nodo <- crear_nodo(I)
extraer_paper(nodo)
}
for(i in b$doi){
print(i)
nodo <- crear_nodo(i)
extraer_paper(nodo)
}
for(i in b$doi){
print(i)
nodo <- crear_nodo(i)
extraer_paper(nodo)
}
rlang::last_trace()
papers <- data.frame()
for (i in issues_url) {
nodo = crear_nodo(i)
df <- extraer_general(nodo)
papers <- rbind(papers,df)
}
extraer_general <- function(nodo) {
# Extraemos el títulos de los articulos
titulo <- a |>
html_elements("[data-testid='block-primitivetitle']") |>
html_text2()
issue <- "Sin Issue"
# Extraemos el enlace relativo doi del articulo
doi <- a |>
html_elements( " a,[target='_blank'], .c-Button--link") |>
html_text()
#filtramos por links que contengan https doi
doi <- doi[grep(pattern="https://doi*",doi)]
#Creamos una tabla con la información general de cada journal
tibble(
titulo = titulo,
doi = doi,
issue = issue
)
}
crear_nodo <- function(url){
# Definimos un user-agent similar al de un navegador real
user_agent_navegador <- paste(
"Mozilla/5.0 (Windows NT 10.0; Win64; x64)",
"AppleWebKit/537.36 (KHTML, like Gecko)",
"Chrome/122.0.0.0 Safari/537.36"
)
# Construimos la solicitud HTTP y añadimos encabezados para
# que parezca una visita normal
respuesta <- request(url) |>
req_user_agent(user_agent_navegador) |>
req_headers(`accept-language` = "en-US,en;q=0.9") |>
req_perform()
# Convertimos el cuerpo de la respuesta en un documento HTML
# analizable con rvest/xml2
nodo <- respuesta |>
resp_body_html()
nodo
}
b <- extraer_general(a)
b
for(u in b$doi){
print(u)
nodo <- crear_nodo(u)
extraer_paper(nodo)
}
rlang::last_trace()
extraer_paper <- function(url){
nodo <- crear_nodo(url)
#EXTRACCIÓN DOI (LLAVE EN COMUN CON GENERALITIES)
doi <- nodo |>
html_elements( " a,[target='_blank'], .c-Button--link") |>
html_text()
doi <- doi[grep(pattern="https://doi*",doi)][1]
#AUTORES
autores_ex <- nodo |>
html_elements("[data-testid='author-name']") |>
html_text()
autores <- paste(autores_ex,collapse = ",")
#ABSTRACT
raw_abstract <- nodo |> html_elements(".abstract.border-bottom.border-bottom-solid.border-bottom-medium") |>
html_text2()
a <- unlist(strsplit(raw_abstract, split = "\n"))
Backround_and_aims <- NA
Methods <- NA
Results <- NA
Conclusion <- NA
abstract <- NA
if (length(a)<4) {
abstract <- a[3]
}else {
Backround_and_aims <- a[4]
Methods <- a[8]
Results <- a[12]
Conclusion <- a[16]
}
#FECHA DE PUBLICACIÓN
fecha <- nodo |>
html_elements(".onlinepubdate.c-List__items") |>
html_text2() |>
str_squish() |>
str_remove("^[^0-9]+")
#Métrica
fwci <- oa_fetch(entity = "works", doi = doi)$fwci
#URL
URL <- nodo |>
html_elements('[rel="canonical"], href')|>
html_attr("href")
#REFERENCIAS
ref_ex <- NA
ref_ex <- nodo |>
html_elements(".citationText.text-body1") |>
html_text()
# CITAS
dyn_cit = read_html_live(URL)
try(dyn_cit$click("[data-tab-id='citedBy-30734']"),silent = TRUE)
n_citaciones <- NA
intentos <- 0
while (intentos < 10) {
Sys.sleep(3)
p <- dyn_cit |>
html_elements("#citedByWidget") |>
html_children()
texto <- p[2] |> html_text() |> str_squish()
if (length(texto) > 0 && nchar(texto) > 0) {
n_citaciones <- word(texto, -2)
break
}
intentos <- intentos + 1
}
paper_row <- tibble (doi = doi,
url = URL,
fecha_publicacion = fecha,
nro_de_citas = n_citaciones,
fwci = fwci)
abstract_row <- tibble( doi = doi,
resumen = abstract,
Backround = Backround_and_aims,
Metodos = Methods,
Resultados = Results,
Conclusion = Conclusion
)
autores_row <- tibble( doi = doi,
autores = list(autores_ex) )
ref_row <- tibble(doi = doi,
list(ref_ex))
dyn_cit$session$close()
return(list(paper_row,abstract_row,autores_row,ref_row))
}
papier <- extraer_paper(nodo)
nodo <- crear_nodo("https://doi.org/10.1556/2006.2025.00590")
papier <- extraer_paper(nodo)
rlang::last_trace()
for(u in b$doi){
print(u)
extraer_paper(u)
}
papier <- extraer_paper("https://doi.org/10.1556/2006.2025.00590")
extraer_paper("https://doi.org/10.1556/2006.2025.00590")
rlang::last_trace()
nodo <- crear_nodo("https://doi.org/10.1556/2006.2025.00590")
nodo <- crear_nodo("https://doi.org/10.1556/2006.2025.00590")
# Definimos un user-agent similar al de un navegador real
user_agent_navegador <- paste(
"Mozilla/5.0 (Windows NT 10.0; Win64; x64)",
"AppleWebKit/537.36 (KHTML, like Gecko)",
"Chrome/122.0.0.0 Safari/537.36"
)
respuesta <- request("https://doi.org/10.1556/2006.2025.00590") |>
req_user_agent(user_agent_navegador) |>
req_headers(`accept-language` = "en-US,en;q=0.9") |>
req_perform()
# Convertimos el cuerpo de la respuesta en un documento HTML
# analizable con rvest/xml2
nodo <- respuesta |>
resp_body_html()
respuesta
#PRECAUCIÓN TIEMPO APROXIMADO DE EJECUCIÓN 40 mins a 1 hora
for (i in papers$doi[1:5]){
tibbles <- extraer_paper(i)
comp_papers <- comp_papers |> bind_rows(tibbles[1])
abstract <- abstract |> bind_rows(tibbles[2])
autores <- autores |> bind_rows(tibbles[3])
referencias <- referencias |> bind_rows(tibbles[4])
it= it +1
setTxtProgressBar(pb, it)
}
extraer_general <- function(nodo) {
# Extraemos el títulos de los articulos
titulo <- a |>
html_elements("[data-testid='block-primitivetitle']") |>
html_text2()
issue <- "Sin Issue"
# Extraemos el enlace relativo doi del articulo
doi <- a |>
html_elements( " a,[target='_blank'], .c-Button--link") |>
html_text()
#filtramos por links que contengan https doi
doi <- doi[grep(pattern="https://doi*",doi)]
#Creamos una tabla con la información general de cada journal
tibble(
titulo = titulo,
doi = doi,
issue = issue
)
}
papers <- data.frame()
for (i in issues_url) {
nodo = crear_nodo(i)
df <- extraer_general(nodo)
papers <- rbind(papers,df)
}
papier <- extraer_paper("https://www.akjournals.com/view/journals/2006/aop/article-10.1556-2006.2025.00590/article-10.1556-2006.2025.00590.xml")
crear_nodo("https://doi.org/10.1556/2006.2025.00590")
