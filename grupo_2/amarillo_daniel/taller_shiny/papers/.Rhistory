shiny::runApp()
runApp()
AirPassengers
output$fcwi_issue <- renderHighchart(
papers
{hchart(AirPassengers, name = "AirPassengers") |>
papers
paquetes <- c(
"RSQLite","knitr","tidyverse","knitr","highcharter","bslib","tidyverse"
)
# Verificamos qué paquetes faltan
instalados <- rownames(installed.packages())
pendientes <- setdiff(paquetes, instalados)
if (length(pendientes) > 0) {
install.packages(pendientes)
}
# Cargamos los paquetes sin mostrar mensajes
lapply(paquetes, library, character.only = TRUE)
#### carga de datos y normalización ####
con <- dbConnect(RSQLite::SQLite(), "JBA_25_26.sqlite")
for( i in dbListTables(con)){
assign(i,dbReadTable(con, i))
}
papers
runApp()
renderHighchart(
q2 <- papers |>
group_by(issue) |>
summarise(fwci_mean = mean(fwci))
papers |>
papers |>
group_by(issue)
papers |>
group_by(issue) |>
summarise(fwci_mean = mean(fwci))
papers
View(papers)
sub("\s.*\s",papers)
sub("/s.*/s",papers)
sub(" .* ",papers)
str_extract(papers$fecha_publicacion," .* ")
str_extract(papers$fecha_publicacion,"\s.*\s")
as.Date(papers$fecha_publicacion,format = "%d %m %y")
as.Date(papers$fecha_publicacion,format = "%d %b %Y")
papers$fecha_publicacion
as.Date(papers$fecha_publicacion[1],format = "%d %b %Y")
paquetes <- c(
"RSQLite","knitr","tidyverse","knitr","highcharter","bslib","tidyverse","lubridate"
)
# Verificamos qué paquetes faltan
instalados <- rownames(installed.packages())
pendientes <- setdiff(paquetes, instalados)
if (length(pendientes) > 0) {
install.packages(pendientes)
}
# Cargamos los paquetes sin mostrar mensajes
lapply(paquetes, library, character.only = TRUE)
#### carga de datos y normalización ####
con <- dbConnect(RSQLite::SQLite(), "JBA_25_26.sqlite")
for( i in dbListTables(con)){
assign(i,dbReadTable(con, i))
}
dmy(papers$fecha_publicacion)
papers$fecha_publicacion <- dmy(papers$fecha_publicacion)
papers |>
group_by(fecha_publicacion) |>
summarise(fwci_mean = mean(fwci))
runApp()
runApp()
runApp()
runApp()
runApp()
runApp()
runApp()
runApp()
runApp()
runApp()
runApp()
runApp()
runApp()
runApp()
runApp()
runApp()
runApp()
runApp()
runApp()
runApp()
runApp()
runApp()
runApp()
runApp()
runApp()
runApp()
runApp()
runApp()
runApp()
runApp()
runApp()
runApp()
papers$fecha_publicacion <- dmy(papers$fecha_publicacion)
runApp()
papers |>
group_by(fecha_publicacion) |>
summarize(media = mean(fwci))
papers
#### carga de datos y normalización ####
con <- dbConnect(RSQLite::SQLite(), "JBA_25_26.sqlite")
for( i in dbListTables(con)){
assign(i,dbReadTable(con, i))
}
papers <- papers |> mutate(issue = str_remove(issue,".*:"))
papers$year <- as.numeric(papers$year)
papers$fecha_publicacion <- dmy(papers$fecha_publicacion)
papers$issue <- factor(papers$issue,c(" Issue 1 (Mar 2025)"," Issue 2 (Jun 2025)",
" Issue 3 (Sep 2025)"," Issue 4 (Dec 2025)",
" Issue 1 (Mar 2026)"),ordered = TRUE)
papers |>
group_by(fecha_publicacion) |>
summarize(media = mean(fwci))
runApp()
q2 <- papers |>
group_by(fecha_publicacion) |>
summarize(media = mean(fwci))
View(q2)
runApp()
hchart(q2,type="line",hcaes(x= fecha_publicacion, y = media), name = "AirPassengers") |>
hc_title(text = "<b>Cifras mensuales de pasajeros de líneas aéreas 1949-1960</b>") |>
hc_subtitle(text = "<i>Dataset AirPassengers de R</i>") |>
hc_credits(enabled = TRUE, text = "http://elartedeldato.com")
runApp()
runApp('~/test')
runApp('~/test')
runApp('~/test')
runApp('~/test')
paste("<b>Relevancia de papers publicados",min(papers$year),"-",max(papers$year),"</b>")
runApp()
runApp()
runApp()
runApp()
runApp()
runApp()
papers[c("nro_de_citas","n_referencia","n_autores","topic_label")]
papers[c("fwci","n_referencia","n_autores","topic_label")]
runApp()
shiny::runApp()
paquetes <- c(
"RSQLite","knitr","tidyverse","knitr","highcharter"
,"bslib","tidyverse","lubridate","DT","bsicons","plotly"
)
# Verificamos qué paquetes faltan
instalados <- rownames(installed.packages())
pendientes <- setdiff(paquetes, instalados)
if (length(pendientes) > 0) {
install.packages(pendientes)
}
# Cargamos los paquetes sin mostrar mensajes
lapply(paquetes, library, character.only = TRUE)
runApp()
runApp()
renderHighchart({
highchart() %>%
hc_chart(type = "gauge", plotBackgroundColor = NULL, plotBackgroundImage = NULL, plotBorderWidth = 0, plotShadow = FALSE) %>%
hc_title(text = "Progreso Actual") %>%
hc_pane(startAngle = -90, endAngle = 90,
background = list(
list(backgroundColor = "#FFF", borderWidth = 0, outerRadius = "109%", innerRadius = "107%")
)) %>%
hc_yAxis(min = 0, max = 100,
minorTickInterval = "auto", minorTickWidth = 1, minorTickLength = 10, minorTickPosition = "inside", minorTickColor = "#666",
tickPixelInterval = 30, tickWidth = 2, tickPosition = "inside", tickLength = 10, tickColor = "#666",
labels = list(step = 2, rotation = "auto"),
title = list(text = "%"),
plotBands = list(
list(from = 0, to = 60, color = "#55BF3B"),    # Verde
list(from = 60, to = 80, color = "#DDDF0D"),   # Amarillo
list(from = 80, to = 100, color = "#DF5353")   # Rojo
)) %>%
hc_add_series(name = "Valor", data = list(input$gauge_val),
tooltip = list(valueSuffix = " %"))
})
highchart() %>%
hc_chart(type = "gauge", plotBackgroundColor = NULL, plotBackgroundImage = NULL, plotBorderWidth = 0, plotShadow = FALSE) %>%
hc_title(text = "Progreso Actual") %>%
hc_pane(startAngle = -90, endAngle = 90,
background = list(
list(backgroundColor = "#FFF", borderWidth = 0, outerRadius = "109%", innerRadius = "107%")
)) %>%
hc_yAxis(min = 0, max = 100,
minorTickInterval = "auto", minorTickWidth = 1, minorTickLength = 10, minorTickPosition = "inside", minorTickColor = "#666",
tickPixelInterval = 30, tickWidth = 2, tickPosition = "inside", tickLength = 10, tickColor = "#666",
labels = list(step = 2, rotation = "auto"),
title = list(text = "%"),
plotBands = list(
list(from = 0, to = 60, color = "#55BF3B"),    # Verde
list(from = 60, to = 80, color = "#DDDF0D"),   # Amarillo
list(from = 80, to = 100, color = "#DF5353")   # Rojo
)) %>%
hc_add_series(name = "Valor", data = list(input$gauge_val),
tooltip = list(valueSuffix = " %"))
papers$fwci
min(papers$fwci)
max(papers$fwci)
runApp()
runApp()
runApp()
runApp()
runApp()
runApp()
runApp()
runApp()
runApp()
runApp()
runApp()
View(abstract)
runApp()
View(paper_references)
View(paper_references)
View(references)
View(papers)
runApp()
hist(paper_references)
frequency(paper_references)
paper_references |> group_by(id_referencia) |> summarize(count = n())
paper_references |> group_by(id_referencia) |> summarize(count = n()) |>sort(decreasing = T)
paper_references |> group_by(id_referencia) |> summarize(count = n()) |>sort(decreasing = TRUE)
paper_references |> group_by(id_referencia) |> summarize(count = n()) |> arrange(desc(count))
runApp()
